const nickname = "Timmy";
const firstname = "Timothy";
const myName = nickname || firstname        // add both const either or
console.log(`Good Morning, ${myName}!`)     // add ` instead of "" and one ${ }